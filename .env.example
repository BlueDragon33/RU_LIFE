# Public URL of Site Quản trị. The default in client code is the current control-center deployment.
NEXT_PUBLIC_CONTROL_CENTER_BASE_URL=https://quan-ly-hoc-tap.dinhnam3391.chatgpt.site

# Must exactly match MEDICINE_SERVICE_SECRET in Site Quản trị.
# Use a random value of at least 32 characters. Never expose it to the browser.
MEDICINE_SERVICE_SECRET=replace-with-the-same-secret-as-learning-management
