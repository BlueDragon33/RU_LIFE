name: RU_LIFE Cloudflare Preview Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ru-life-cloudflare-preview
  cancel-in-progress: true

jobs:
  deploy-preview:
    runs-on: ubuntu-24.04
    timeout-minutes: 18
    environment: ru-life-preview
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      RU_LIFE_PREVIEW_D1_DATABASE_ID: ${{ secrets.RU_LIFE_PREVIEW_D1_DATABASE_ID }}
      RU_LIFE_PRODUCTION_D1_DATABASE_ID: ${{ secrets.RU_LIFE_PRODUCTION_D1_DATABASE_ID }}
      APPLICATION_MANAGEMENT_PREVIEW_ORIGIN: ${{ vars.APPLICATION_MANAGEMENT_PREVIEW_ORIGIN }}
      RU_LIFE_PREVIEW_ORIGIN: ${{ vars.RU_LIFE_PREVIEW_ORIGIN }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22.13.0"

      - name: Detect preview readiness
        id: config
        shell: bash
        run: |
          set -euo pipefail
          missing=()
          [[ -n "${CLOUDFLARE_API_TOKEN:-}" ]] || missing+=("CLOUDFLARE_API_TOKEN")
          [[ -n "${CLOUDFLARE_ACCOUNT_ID:-}" ]] || missing+=("CLOUDFLARE_ACCOUNT_ID")
          [[ -n "${RU_LIFE_PREVIEW_D1_DATABASE_ID:-}" ]] || missing+=("RU_LIFE_PREVIEW_D1_DATABASE_ID")
          [[ -n "${RU_LIFE_PRODUCTION_D1_DATABASE_ID:-}" ]] || missing+=("RU_LIFE_PRODUCTION_D1_DATABASE_ID")
          if (( ${#missing[@]} )); then
            echo "ready=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Development preview skipped; missing: ${missing[*]}"
          else
            echo "ready=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Install dependencies
        if: steps.config.outputs.ready == 'true'
        run: npm install --no-audit --no-fund

      - name: Package preview
        if: steps.config.outputs.ready == 'true'
        run: npm run cloudflare:preview:prepare

      - name: Apply preview migrations
        if: steps.config.outputs.ready == 'true'
        run: npx wrangler d1 migrations apply ru-life-preview-db --remote --config wrangler.cloudflare.preview.jsonc

      - name: Build preview
        if: steps.config.outputs.ready == 'true'
        run: CLOUDFLARE_VITE_WRANGLER_CONFIG_PATH=wrangler.cloudflare.preview.jsonc npm run build

      - name: Deploy preview
        if: steps.config.outputs.ready == 'true'
        run: npx wrangler deploy

      - name: Quick availability check
        if: steps.config.outputs.ready == 'true' && env.RU_LIFE_PREVIEW_ORIGIN != ''
        shell: bash
        run: |
          set -euo pipefail
          for i in {1..10}; do
            code="$(curl --silent --show-error --output /dev/null --write-out '%{http_code}' --max-time 10 "$RU_LIFE_PREVIEW_ORIGIN/" || true)"
            case "$code" in 200|301|302|303|307|308|401|403) echo "Preview reachable: HTTP $code"; exit 0 ;; esac
            sleep 2
          done
          echo "::warning::Deploy completed but configured preview URL did not answer yet."

      - name: Development publish summary
        if: always()
        run: |
          if [[ "${{ steps.config.outputs.ready }}" == "true" ]]; then
            echo "Development preview publish path executed."
          else
            echo "Development continues without blocking: Cloudflare preview credentials/bindings are not configured in this repository."
          fi
